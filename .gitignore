# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependency directories (This keeps the actual modules out of Git)
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Electron-specific build outputs
# (Adjust these depending on if you use electron-builder, electron-forge, etc.)
dist/
out/
build/
release/

# Environment variables (Never commit secrets!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
